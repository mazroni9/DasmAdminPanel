# Laravel API (same host as production backend, e.g. Render). No trailing slash.
NEXT_PUBLIC_PLATFORM_API_URL=https://api.dasm.com.sa

# (اختياري) نفس عنوان الـ API للاستخدام من طبقة BFF على الخادم — إن لم يُضبط يُستخدم NEXT_PUBLIC_PLATFORM_API_URL
# DASM_PLATFORM_API_URL=https://api.dasm.com.sa

# Main platform (Next) for "لوحة الإدارة" link from control room; no trailing slash.
NEXT_PUBLIC_MAIN_FRONTEND_URL=https://www.dasm.com.sa
